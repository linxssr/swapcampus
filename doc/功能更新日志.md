# SwapCampus 功能更新日志

## 2026年6月（第三次）- 基于浏览历史的个性化推荐

### 一、功能概述

新增"猜你喜欢"个性化推荐功能。用户浏览商品详情时自动记录行为，后端基于分类偏好分析返回定制推荐列表，在首页"最新发布"下方展示（仅登录用户可见）。

---

### 二、数据库

#### 新增表：item_browse_history（商品浏览历史）

| 字段 | 类型 | 说明 |
|------|------|------|
| history_id | bigint PK | 自增主键 |
| user_id | bigint FK | 用户 ID |
| item_id | bigint FK | 商品 ID |
| category_id | bigint | 分类 ID（冗余，便于统计） |
| browse_time | datetime | 最近浏览时间 |

唯一索引 `uk_user_item(user_id, item_id)`：同一用户对同一商品只保留一条记录，重复浏览更新 `browse_time`。

**执行 SQL（详见数据库设计.md 第四节）**

---

### 三、后端

#### 1. BrowseHistoryMapper

新建 `swapcampus-mapper/BrowseHistoryMapper.java`：
- `upsert(userId, itemId, categoryId, browseTime)` — INSERT ON DUPLICATE KEY UPDATE
- `topCategories(userId, limit)` — 统计最近 N 条浏览中各分类出现次数，降序
- `browsedItemIds(userId)` — 返回用户所有已浏览商品 ID（用于过滤）

#### 2. ItemService 新增方法

```java
void recordBrowse(Long userId, Long itemId);
List<ItemVO> getRecommendItems(Long userId, int limit);
```

#### 3. ItemServiceImpl 推荐逻辑

```
1. 查最近 30 条浏览 → 统计 top3 分类及权重
2. 按权重为每个分类分配配额（共 limit 条）
3. 各分类取最新在售商品，过滤：排除已浏览、排除自己发布的
4. 随机打散结果，避免全是同一分类
5. 结果不足时用最新商品补足
```

#### 4. ItemController 新增接口

| 接口 | 说明 |
|------|------|
| `POST /api/v1/item/browse/{itemId}` | 记录浏览（需登录，幂等） |
| `GET /api/v1/item/recommend?limit=8` | 获取推荐列表（需登录） |

---

### 四、前端

#### 1. api/modules/item.ts
新增两个 API 函数：`recordBrowse(itemId)` 和 `getRecommendItems(limit)`

#### 2. composables/useHistory.ts（新文件）
封装浏览历史的双写逻辑：
- 写 localStorage（无论是否登录，保留最近 50 条）
- 登录状态下同步上报 `POST /item/browse/{itemId}`
- 失败静默处理，不影响用户体验

#### 3. ItemDetailView.vue
在 `loadItem()` 成功后调用 `track(itemId, categoryId)` 记录浏览。

#### 4. HomeView.vue
- 新增 `recommendItems` 状态和 `loadRecommendItems()` 函数
- `onMounted` 并行加载最新发布和推荐（`Promise.all`）
- 新增"猜你喜欢"区块（8 条），位于"最新发布"下方
  - 仅登录用户可见
  - 推荐为空时隐藏整个区块
  - 标题使用 ✦ 图标（与最新发布的 ▶ 区分）
  - 加载中显示骨架屏

---

### 五、文件变更清单

| 文件 | 类型 | 说明 |
|------|------|------|
| `doc/数据库设计.md` | 更新 | 新增 item_browse_history 表说明 + DDL |
| `swapcampus-mapper/BrowseHistoryMapper.java` | 新建 | 浏览历史 Mapper |
| `module-m2-item/ItemService.java` | 更新 | 新增两个方法声明 |
| `module-m2-item/ItemServiceImpl.java` | 更新 | 注入 BrowseHistoryMapper，实现推荐逻辑 |
| `module-m2-item/ItemController.java` | 更新 | 新增两个接口 |
| `frontend/api/modules/item.ts` | 更新 | 新增 recordBrowse / getRecommendItems |
| `frontend/composables/useHistory.ts` | 新建 | 浏览历史 composable |
| `frontend/views/user/item/ItemDetailView.vue` | 更新 | 进入页面时调用 track() |
| `frontend/views/user/home/HomeView.vue` | 更新 | 新增猜你喜欢区块 |

---

### 六、使用流程

1. 登录后访问任意商品详情页 → 自动记录浏览（localStorage + 后端）
2. 回到首页 → "猜你喜欢"区块显示基于浏览分类偏好的推荐商品
3. 浏览历史不足或推荐结果为空 → 区块隐藏，不影响首页正常展示

### 七、部署说明

需手动执行新建表 SQL（见数据库设计.md 第四节），后端重启后功能生效。

---



## 2026年6月（第二次）- 全局 UI 像素风改造 & 图片修复

### 一、全局像素风设计系统

**设计理念：** 复古像素风 + 低饱和度护眼色 + 极简主义，主色调 `#9DBF9E`（薄荷绿），背景 `#F9F7F1`（米白），文字 `#2D3047`（深藏青）。

**核心变化（`index.css`）：**
- 建立完整 CSS 变量系统：`--color-primary`、`--shadow-hard`、`--border-pixel` 等
- 硬阴影规则：`box-shadow: 4px 4px 0px #2D3047`，无模糊，hover 时位移 2px 模拟按压
- 全量覆盖 Element Plus 组件样式：按钮、输入框、表格、标签、弹窗、分页、下拉框等统一像素风

**改动文件：**
- `src/assets/styles/index.css` — 全局样式重写，建立设计系统
- `src/layouts/UserLayout.vue` — 导航栏像素风：品牌名加 ⚔ 图标，激活链接变薄荷绿色块
- `src/layouts/AdminLayout.vue` — 管理后台深色 Header + 薄荷绿品牌色，侧边栏激活项左边框指示
- `src/views/user/home/HomeView.vue` — Hero Banner 改薄荷绿底 + 像素网格纹路，分类卡片/商品卡片硬阴影
- `src/views/user/item/ItemListView.vue` — 筛选侧栏硬阴影，价格标签改深底黄字
- `src/views/user/item/ItemDetailView.vue` — 价格区域加背景框，卖家卡片硬阴影，评价区硬边框
- `src/views/user/auth/LoginView.vue` — 登录页背景改米白+网格纹，卡片像素硬阴影
- `src/views/user/auth/RegisterView.vue` — 同上
- `src/views/user/profile/ProfileView.vue` — 用户卡片硬阴影，信用分显示为麦穗黄背景标签
- `src/views/admin/auth/AdminLoginView.vue` — 深色背景+像素网格，薄荷绿硬阴影边框，新增普通用户登录/注册跳转链接

---

### 二、聊天界面像素风改造

#### 1. 会话侧边栏（ChatView.vue）
- 整体切换为浅色像素风，与全局设计系统统一
- 头像从圆形蓝色渐变改为方形硬边框
- 激活会话用左侧 3px 薄荷绿色块指示
- 未读角标改为方形 + 硬边框
- 顶部标题区使用 `#f0ede6` 底色

#### 2. 聊天室主区域（ChatRoomView.vue）
- 消息背景改为米白 `#F9F7F1` + 淡像素网格
- Header 和底部输入栏使用 `#f0ede6`
- 气泡带三角指针（RPG 对话框风格），对方白底深边框，自己薄荷绿底
- 输入框改为白底深色文字，焦点时薄荷绿边框
- 工具按钮、表情面板、发送按钮全部改为浅色像素风

---

### 三、其他功能修复

#### 1. 个人中心头像修复（ProfileView.vue）
- 修复头像写死 `icon="UserFilled"` 的问题
- 改为：有头像时显示图片，无头像时显示默认图标

#### 2. 导航栏头像修复（UserLayout.vue）
- 同上，修复导航栏用户头像未显示的问题

#### 3. 首页吉祥物图标
- 新增 `src/assets/images/闲鸟.png` 像素鸟吉祥物
- 放置于 Hero Banner 标题左侧，`image-rendering: pixelated` 保持像素清晰度

#### 4. 首页"最新发布"排序修复（HomeView.vue）
- 原逻辑：只取前三个分类的商品，未排序直接截取
- 修复：改为 `filterItems({})` 获取全量商品，按 `createTime` 降序排列后取前 8 条

#### 5. 商品分类 Emoji 修复（HomeView.vue）
- 原代码 key 与数据库实际分类名不匹配，全部显示 📦
- 补全所有实际分类名对应的 emoji 映射

#### 6. 图片跨域修复（request.ts / upload.ts）
- 问题：数据库旧数据存储 `http://localhost:9000/...` 绝对地址，浏览器跨域被 ORB 拦截
- 修复：在 Axios 响应拦截器中自动将图片绝对地址转为走 Vite 代理的相对路径 `/minio/...`
- 新增 `toProxiedUrl()` 工具函数，处理 `coverUrl`、`imageUrls`、`avatar`、`peerAvatar`、`sellerAvatar` 等字段
- 新上传的图片（已是 `/minio/...` 相对路径）不受影响

#### 7. TypeScript .vue 模块声明（env.d.ts）
- 补全 `declare module '*.vue'` 声明，修复 TS 报"找不到模块"错误

---

### 四、文件变更清单

| 文件 | 变更类型 | 说明 |
|------|------|------|
| `src/assets/styles/index.css` | 重写 | 全局设计系统 |
| `src/assets/images/闲鸟.png` | 新增 | 像素鸟吉祥物 |
| `src/env.d.ts` | 修复 | 补全 .vue 模块声明 |
| `src/utils/upload.ts` | 新增函数 | `toProxiedUrl()` |
| `src/utils/request.ts` | 新增逻辑 | 响应拦截器自动重写图片 URL |
| `src/layouts/UserLayout.vue` | 样式改造 | 像素风导航栏，头像修复 |
| `src/layouts/AdminLayout.vue` | 样式改造 | 深色 Header + 像素风侧边栏 |
| `src/views/user/home/HomeView.vue` | 样式+逻辑 | 像素风 + 最新发布排序 + 分类 Emoji + 吉祥物 |
| `src/views/user/item/ItemListView.vue` | 样式改造 | 像素风商品列表 |
| `src/views/user/item/ItemDetailView.vue` | 样式改造 | 像素风商品详情 |
| `src/views/user/auth/LoginView.vue` | 样式改造 | 像素风登录页 |
| `src/views/user/auth/RegisterView.vue` | 样式改造 | 像素风注册页 |
| `src/views/user/profile/ProfileView.vue` | 样式+修复 | 像素风个人中心，头像修复 |
| `src/views/user/chat/ChatView.vue` | 样式改造 | 像素风会话侧边栏 |
| `src/views/user/chat/ChatRoomView.vue` | 样式改造 | 浅色像素风聊天室 |
| `src/views/admin/auth/AdminLoginView.vue` | 样式+功能 | 像素风管理员登录，新增用户端跳转链接 |

---



## 2026年6月 - 评价与用户主页功能完善

### 一、评价功能增强

#### 1. 订单详情显示评价历史

**前端改动：**
- `OrderListView.vue` - 订单详情弹窗新增"交易评价"区域
  - 点击"详情"按钮时自动加载该订单的评价
  - 显示评价人、评分星级、评价内容和时间
  - 如果没有评价，显示"该订单暂无评价"

**后端新增接口：**
```
GET /api/v1/comment/order/{orderId}
```
- 返回指定订单的评价信息
- 包含买家名称、评分、评价内容、评价时间

**实现文件：**
- `CommentController.java` - 新增 `getCommentByOrderId()` 方法
- `CommentService.java` / `CommentServiceImpl.java` - 实现评价查询逻辑
- `CommentMapper.java` - 新增 `selectByOrderId()` 查询方法

#### 2. 商品详情显示所有评价

**前端改动：**
- `ItemDetailView.vue` - 在商品详情页底部新增"商品评价"区域
  - 显示该商品的所有历史评价（评价数量）
  - 每条评价显示：评价人、评分星级、评价内容、评价时间
  - 加载商品详情时自动加载评价列表
  - 如果没有评价，显示"暂无评价"

**后端改动：**
- `CommentMapper.java` - 更新 `selectByItemId()` 查询
  - JOIN `user` 表获取买家名称（`buyerName`）
  - 返回 `Map<String, Object>` 格式，字段使用 camelCase

**实现文件：**
- `CommentMapper.java` - SQL 查询添加 `LEFT JOIN user` 获取评价人信息
- `CommentController.java` - `/comment/item/{itemId}` 返回完整评价信息

#### 3. 评价功能完整流程

1. 买家在"我的订单"页面对已完成订单进行评价
2. 提交评价后：
   - 评价保存到数据库
   - 卖家信用分变动（好评≥4星 +5分，差评<4星 -5分）
   - 防止重复评价（后端校验）
3. 评价显示位置：
   - 订单详情弹窗 - 显示该订单的评价
   - 商品详情页底部 - 显示该商品的所有评价

---

### 二、用户主页功能

#### 1. 功能概述

新增用户个人主页功能，支持查看任意用户的公开信息、发布商品和评价历史。区分两种访问场景：
- 访问自己的账号 → 跳转到完整个人中心 (`/profile`)
- 访问他人账号 → 显示公开主页 (`/user/:userId`)

#### 2. 后端新增接口

**获取用户公开信息：**
```
GET /api/v1/user/profile/{userId}
```
- 返回：`userId`、`username`、`creditScore`、`avatar`
- 无需登录，公开接口

**获取用户发布的商品：**
```
GET /api/v1/item/user/{userId}
```
- 返回指定用户发布的所有商品列表
- 无需登录，公开接口

**获取卖家评价历史：**
```
GET /api/v1/comment/seller/{sellerId}
```
- 返回该卖家收到的所有评价
- 包含：买家名称、商品标题、评分、评价内容、评价时间

**实现文件：**
- `UserController.java` - 已有 `getUserProfile()` 方法
- `ItemController.java` - 新增 `getUserItems()` 方法
- `CommentController.java` - 新增 `getSellerComments()` 方法
- `CommentServiceImpl.java` - 实现 `getCommentsBySellerId()` 逻辑
- `CommentMapper.java` - 新增 `selectBySellerId()` 查询，JOIN `user` 和 `item` 表

#### 3. 前端实现

**新建组件：**
- `UserHomeView.vue` - 用户主页组件（`/user/:userId`）

**组件功能：**

1. **用户信息卡片**
   - 头像、昵称
   - 信用分星级展示（90+为5星，70+为4星，依此类推）
   - "联系 TA"按钮（跳转聊天）
   - 如果是自己，显示"前往我的个人中心"按钮

2. **Tab 切换区域**

   **Tab 1: TA 的发布**
   - 商品卡片网格展示
   - 显示商品封面、标题、价格、状态（在售/已下架）
   - 点击商品卡片跳转商品详情

   **Tab 2: 信用与评价**
   - 信用分统计：累计评价数、好评数、差评数、平均评分
   - 评价列表：
     - 每条评价显示买家头像、昵称、评价时间
     - 商品名称（带图标）
     - 评分星级（黄色五星）
     - 评价内容

**前端 API 方法：**
- `getUserProfile(userId)` - 获取用户公开信息
- `getUserPublishedItems(userId)` - 获取用户发布的商品
- `getUserSellerComments(userId)` - 获取卖家评价历史

**路由配置：**
```typescript
{
  path: 'user/:userId',
  name: 'UserHome',
  component: () => import('@/views/user/profile/UserHomeView.vue'),
}
```

#### 4. 商品详情页改造

**卖家信息区域交互优化：**
- 卖家信息卡片整体可点击
- 鼠标悬停有高亮效果（背景变浅、显示阴影）
- 右侧添加 `>` 箭头图标提示可点击
- 点击后跳转到卖家用户主页 `/user/{sellerId}`

**实现文件：**
- `ItemDetailView.vue` - 更新卖家信息区域
  - 新增 `handleViewSeller()` 方法
  - 卡片添加 `@click` 事件和 hover 样式
  - 导入 `ArrowRight` 图标

---

### 三、数据库字段优化

#### CommentMapper 查询优化

**原有问题：**
- `selectByItemId()` 和 `selectBySellerId()` 返回的字段是 snake_case（`buyer_id`、`item_title`）
- 前端期望 camelCase 格式

**解决方案：**
- 使用 SQL `AS` 别名将查询结果字段转为 camelCase
- 示例：
```sql
SELECT c.comment_id AS commentId, c.buyer_id AS buyerId,
       u.username AS buyerName, i.title AS itemTitle
FROM comment c
LEFT JOIN user u ON u.user_id = c.buyer_id
LEFT JOIN item i ON i.item_id = c.item_id
```

---

### 四、前端类型定义完善

**新增类型：**

```typescript
// types/order.ts 更新
export interface CommentRecord {
  commentId: number;
  orderId: number;
  buyerId: number;
  sellerId: number;
  itemId: number;
  buyerName?: string;    // 新增
  itemTitle?: string;    // 新增
  content: string;
  score: number;
  createTime: string;
}
```

---

### 五、技术亮点

1. **前后端字段命名统一** - 后端通过 SQL 别名返回 camelCase，前端直接使用
2. **公开信息与私有信息分离** - 用户主页只显示公开信息（无手机号、学号）
3. **智能路由判断** - 访问自己时引导至完整个人中心
4. **评价统计实时计算** - 前端 computed 自动计算好评率、平均分
5. **用户体验优化** - 卡片悬停效果、加载骨架屏、空状态提示

---

### 六、测试要点

#### 评价功能测试

1. 创建订单 → 确认收货（状态变为"已完成"）→ 提交评价
2. 在订单详情弹窗中查看评价历史
3. 在商品详情页底部查看该商品的所有评价
4. 尝试重复评价，确认后端返回"该订单已评价"错误
5. 验证评价后卖家信用分变动（好评+5，差评-5）

#### 用户主页测试

1. 访问商品详情页，点击卖家信息卡片，跳转到卖家主页
2. 查看"TA 的发布" Tab，点击商品卡片跳转商品详情
3. 查看"信用与评价" Tab，验证评价统计和列表显示
4. 点击"联系 TA"按钮，跳转到聊天页面
5. 访问自己的用户主页 `/user/{自己的userId}`，验证显示"前往我的个人中心"按钮

---

### 七、文件变更清单

#### 后端（7个文件）

1. `CommentController.java` - 新增 2 个接口
2. `CommentService.java` - 新增 2 个方法声明
3. `CommentServiceImpl.java` - 实现评价查询逻辑
4. `CommentMapper.java` - 新增 2 个查询方法，优化字段别名
5. `ItemController.java` - 新增用户商品查询接口
6. `UserController.java` - 已有用户公开信息接口（无改动）
7. `OrderVO.java` - 新建订单视图对象（可选）

#### 前端（8个文件）

1. `types/order.ts` - 更新 `CommentRecord` 类型定义
2. `api/modules/order.ts` - 新增 2 个 API 方法
3. `api/modules/user.ts` - 新增 1 个 API 方法
4. `OrderListView.vue` - 订单详情弹窗显示评价
5. `ItemDetailView.vue` - 底部显示商品评价 + 卖家卡片可点击
6. `UserHomeView.vue` - 新建用户主页组件
7. `router/modules/user.ts` - 新增 `/user/:userId` 路由
8. `ItemDetailView.vue` (样式) - 卖家卡片交互样式

---

### 八、后续优化建议

1. **评价图片** - 支持评价时上传商品实拍图
2. **评价回复** - 卖家可对买家评价进行回复
3. **评价筛选** - 商品详情页支持按好评/差评筛选
4. **用户关注** - 支持关注用户，查看关注列表
5. **隐私设置** - 允许用户隐藏部分公开信息
6. **举报评价** - 支持举报不实或恶意评价

---

## 部署说明

重启后端服务后，所有新功能立即生效。前端无需额外配置，刷新页面即可使用。
